---
name: '02 👊🏽 Auto version bump'

# yamllint disable-line rule:truthy
on:
  workflow_run:
    workflows: [ '01 🚧 Build and test' ]
    types:
      - completed

permissions:
  contents: write  # Required to push commits/tags directly to main

jobs:
  # ✅ Commitizen + Maven versioning in sync
  # ✅ Automatic changelog updates
  # ✅ Clean semver tags (vX.Y.Z)
  # ✅ feat: or fix: commit → CI automation run bumps version, syncs .cz.toml, updates POMs, creates + pushes tag
  bump-version:
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.actor != 'renovate[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0  # Needed for commit range checks
          ref: main  # check out the branch, not detached HEAD
          token: ${{ secrets.PAT_FORGE_DEPLOY }}  # Use PAT so pushed tags trigger workflows
      - uses: ./.github/actions/tools
      - name: Check and bump version
        id: version-bump
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git fetch --tags
          git checkout main
          git pull --ff-only origin main

          CURRENT_VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
          echo "current_version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
          echo "Current version: $CURRENT_VERSION"

          # Sync .cz.toml to current Maven version
          sed -i "s/^version = \".*\"/version = \"$CURRENT_VERSION\"/" .cz.toml

          # Let commitizen do the work - it will update changelog and .cz.toml
          # If no bump is needed, it will exit with non-zero and we skip
          BUMPED=false
          if LEFTHOOK=0 cz bump --yes --changelog 2>&1; then
            # Read the new version that commitizen calculated
            NEW_VERSION=$(grep '^version = ' .cz.toml | sed 's/^version = "\(.*\)"/\1/')
            echo "Version bumped: $CURRENT_VERSION -> $NEW_VERSION"

            # Update Maven to match
            mvn versions:set -DnewVersion=$NEW_VERSION -DgenerateBackupPoms=false
            mvn versions:commit

            # Commit everything (changelog, .cz.toml, pom files)
            git add -A
            git commit -m "chore(release): bump version to $NEW_VERSION [skip ci]" --signoff --no-verify
            git tag -f v$NEW_VERSION

            echo "bumped=true" >> $GITHUB_OUTPUT
            echo "final_version=$NEW_VERSION" >> $GITHUB_OUTPUT
          else
            echo "No version bump needed - no eligible commits found."
            echo "bumped=false" >> $GITHUB_OUTPUT
            echo "final_version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
          fi
      - if: steps.version-bump.outputs.bumped == 'true'
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.PAT_FORGE_DEPLOY }}
          branch: main
          tags: true
          force: false
      - name: Version bump summary
        if: always()
        run: |
          {
            echo "## 📦 Version Bump Status"
            echo ""
            echo "**Current Version:** \`${{ steps.version-bump.outputs.current_version }}\`"
            if [ "${{ steps.version-bump.outputs.bumped }}" = "true" ]; then
              echo ""
              echo "✅ **Version bumped:** \`${{ steps.version-bump.outputs.current_version }}\` → \`${{ steps.version-bump.outputs.final_version }}\`"
              echo ""
              echo "Tag \`v${{ steps.version-bump.outputs.final_version }}\` has been created and pushed."
            else
              echo ""
              echo "ℹ️ **No version bump needed** - no eligible commits found."
            fi
          } >> $GITHUB_STEP_SUMMARY
